I have a DESIGN.md file in the repo root — please read it fully before
making any changes. It defines the visual/information-architecture
direction for this app (colors, type, spacing, and — most importantly —
the layout rules per screen).

CONTEXT
The current deployed app (real-time-airfare.vercel.app) has two tabs:
a MoSPI/analyst tab and a "Should I book?" traveller tab. Both tabs
currently stack ~9 sections vertically with equal visual weight and
default filters set to "all" — this makes the app feel like a data dump
rather than a tool. DESIGN.md's core principle is: every screen leads
with ONE hero answer, everything else is progressive disclosure.

DO NOT touch any data-fetching, Supabase queries, scraper code, or the
`fares`/`apix_daily` schema. This is a layout/UI restructuring task only.
Preserve all existing sample-data fallback behavior exactly as-is.

CHANGES TO MAKE

1. TRAVELLER TAB — rebuild as the priority:
   a. Add a "verdict line" component at the top: one plain-language
      sentence stating whether the currently selected route is cheap or
      expensive right now vs its 30-day average (e.g. "DEL-BOM is 12%
      below its 30-day average — good time to book"). Compute this from
      existing fare data, don't hardcode it.
   b. Replace the "all routes" default filter with a single-route
      selector (pills, not a multi-select dropdown), defaulting to
      whichever route has the most records in the data.
   c. Build ONE new hero chart: for the selected route, one line/bar
      per source (currently cleartrip + indigo), fare over the selected
      time window. This replaces the current disconnected pairing of
      "index movement" (basket-wide) and "route basket" (per-route
      table) — merge their intent into this single chart.
   d. Move "advance-booking fare curve" and "late-booking premium" for
      this tab below the fold, inside a collapsed section (default
      closed) rather than always-visible.
   e. Remove all pipeline/extractor content (pipeline runs, latest
      extractions, extractor head-to-head) from this tab entirely — that
      content moves to the analyst tab only (see below). If any of it
      is currently only implemented on this tab, migrate the component,
      don't duplicate it.
   f. No raw data tables should be visible by default on this tab.

2. MoSPI / ANALYST TAB — reorganize, don't rebuild:
   a. Keep the existing hero (basket index value + sparkline) and the
      back-test overlay (APIx vs DGCA) exactly as-is, in that order,
      always visible — these are the credibility-proof sections and
      should stay prominent.
   b. Move fare composition, late-booking heatmap, extractor
      head-to-head, pipeline runs, and latest extractions into a single
      collapsed section titled "Methodology & pipeline" (default
      closed). Use a simple expand/collapse — no need for tabs within
      tabs.
   c. Keep the "show data tables" toggle pattern that already exists,
      just make sure it lives inside this collapsed section now rather
      than at the top level.

3. GLOBAL
   a. Apply the DESIGN.md color tokens, type system (IBM Plex Mono for
      all numeric values, IBM Plex Sans for headings/body), and spacing
      rules consistently across both tabs — audit existing components
      for any that don't already follow this and fix them.
   b. All delta values (percentages, price changes) must be signed
      (+/-), color-coded per the up/down tokens in DESIGN.md, and paired
      with a stated comparison basis in nearby text (e.g. "vs last
      week") — audit existing delta displays and fix any that show a
      bare number.
   c. Keep the existing "Sample data" banner behavior unchanged.

VERIFICATION
After making changes, list which existing components were reused vs
newly built, and flag anything in DESIGN.md you weren't able to fully
implement (e.g. if computing the "verdict line" needs a data shape that
doesn't exist yet) so I know what's a design-file update vs a data-layer
gap.

Do this incrementally — traveller tab first, confirm it renders
correctly with the current sample data, then move to the analyst tab
reorganization.